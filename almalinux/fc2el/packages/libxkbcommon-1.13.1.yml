target: libxkbcommon
target_ver: 1.13.1
target_fc_rel: 2.fc44
target_el_rel: 2.el10

target_arch: x86_64

target_srpm: "{{ target }}-{{ target_ver }}-{{ target_fc_rel }}.src.rpm"
target_spec: "{{ target }}.spec"
target_url:  "https://kojipkgs.fedoraproject.org//packages/{{ target }}/{{ target_ver }}/{{ target_fc_rel }}/src/{{ target_srpm }}"

target_rpms:
  - "{{ ansible_user_home }}/rpmbuild/RPMS/{{ target_arch }}/{{ target }}-{{ target_ver }}-{{ target_el_rel }}.{{ target_arch }}.rpm"

